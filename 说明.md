# Beike_AspectD 兼容 Flutter 3.35.7 说明

## 前置条件

- Flutter 3.35.7（Dart SDK 3.9.2）已通过 fvm 安装
- `fvm use 3.35.7`

---

## 使用步骤

### 步骤 1：编译 AOT Snapshot

```bash
cd /path/to/Beike_AspectD
dart compile aot-snapshot inner/flutter_frontend_server/starter.dart \
  -o inner/flutter_frontend_server/frontend_server_aot.dart.snapshot
```

> 必须使用与 Flutter SDK 同版本的 Dart（3.9.2）编译，否则 dartaotruntime 无法加载。

### 步骤 2：应用 flutter_tools 补丁

```bash
bash inner/apply_patch.sh
```

此脚本向 Flutter SDK 的 `flutter_tools` 注入 `aspectd.dart`，使 `flutter build` 时自动：
1. 检测项目是否依赖 `beike_aspectd`
2. 用项目中的 `frontend_server_aot.dart.snapshot` 替换 SDK 默认的
3. 传递 `--aop 1` 参数启用 AOP 转换

### 步骤 3：项目集成

在 Flutter 项目 `pubspec.yaml` 中添加依赖：

```yaml
dependencies:
  beike_aspectd:
    path: /path/to/Beike_AspectD  # 或 git 依赖
```

### 步骤 4：编写 AOP 注解

参考 `example/` 项目中的用法。

### 步骤 5：构建

```bash
flutter build apk --release
# 或
flutter build bundle
```

首次构建输出 `aop: need perform true` 即确认 AOP 转换已启用。

---

## 本次适配关键改动

| 文件 | 改动内容 |
|------|----------|
| `pubspec.yaml` / `example/pubspec.yaml` | SDK 约束改为 `>=3.9.0 <4.0.0` |
| `inner/pubspec.yaml` | SDK 约束指向 3.9.2 |
| `inner/pkg/`（16 个包） | 从 Dart 3.9.2 源码整体替换 |
| `inner/flutter_frontend_server/server.dart` | 适配新 API（`FlutterTarget`） |
| `inner/apply_patch.sh` | snapshot 文件名改为 `frontend_server_aot.dart.snapshot` |
| `transformer/` 4 处 | `Nullability.legacy` → `Nullability.nonNullable` |
| `transformer/` 1 处 | `CallSiteAttributesMetadata` 导入路径更新为 `vm/modular/metadata/` |
| `transformer/` 1 处 | `List<int>` → `Uint8List` |
| `aop_addimpl_transformer.dart:155` | 新增 `procedure.parent = pointCutClass`（修复 release 崩溃） |
| `aop_executeimpl_transformer.dart:255-258` | 新增 parent 设置 |
| `example/lib/main.dart` | `bodyText2` → `bodyMedium`（适配新 Flutter API） |

---

## 注意事项

1. **AOT snapshot 版本必须匹配** — 用 Flutter SDK 内置的 `dart` 编译，版本不一致会报 `Wrong full snapshot version`
2. **首次运行需删除缓存** — `rm $FLUTTER_ROOT/bin/cache/flutter_tools.snapshot`，之后自动重建
3. **Release 模式独有问题** — Debug 模式不触发 `ConstantsTransformer`，parent 为 null 的问题仅在 release 构建暴露
4. **snapshot 格式变更** — 旧版使用 `frontend_server.dart.snapshot`（kernel），3.35.7 使用 `frontend_server_aot.dart.snapshot`（ELF AOT）
5. **Nullability.legacy 已移除** — Dart 3.9.2 不再支持 null-safety 迁移期，所有代码强制 null-safe

---

## 如何调试

### 原理

Beike_AspectD 主要修改了 Flutter 的两个流程：
- **flutter_tools** — 负责检测 AOP 配置并替换 frontend_server snapshot
- **frontend_server** — 负责执行 AOP 转换（kernel AST 操作）

### 一、调试 flutter_tools

目的：获取调用 frontend_server 的完整参数。

#### 1. 获取 flutter_tools 启动参数

在 IDE 中以 debug 模式启动 flutter（或观察 `--verbose` 输出）：

```bash
cd /path/to/example
flutter run --release --verbose 2>&1 | grep "dart.*frontend_server"
```

控制台输出类似：
```
/path/to/flutter/bin/cache/dart-sdk/bin/dart --enable-asserts ... /path/to/flutter/packages/flutter_tools/bin/flutter_tools.dart --no-color run --machine --track-widget-creation --device-id=XXXXX --start-paused --release lib/main.dart
```

#### 2. 在 IDE 中调试 flutter_tools

打开 `flutter/packages/flutter_tools` 目录，创建 **Dart Command Line App** 配置：

| 配置项 | 值 |
|--------|------|
| Dart file | `flutter_tools.dart` |
| Program arguments | `--no-color run --release --verbose lib/main.dart` |
| Working directory | Example 工程根目录 |

点击 Debug 开始调试。在输出中搜索 `frontend_server` 获取完整参数。

### 二、调试 frontend_server（AOP 核心）

目的：断点调试 AOP transformer 的 kernel AST 转换逻辑。

#### 1. 获取 frontend_server 参数

从上一步的 verbose 输出中提取 frontend_server 调用参数，示例：

```
/path/to/flutter/bin/cache/dart-sdk/bin/dartaotruntime /path/to/frontend_server_aot.dart.snapshot --sdk-root /path/to/flutter_patched_sdk_product/ --target=flutter --no-print-incremental-dependencies --aot --tfa --packages /path/to/example/.dart_tool/package_config.json --output-dill /path/to/app.dill --depfile /path/to/kernel_snapshot.d package:example/main.dart
```

#### 2. 在 IDE 中调试

打开 Beike_AspectD 工程，创建 **Dart Command Line App** 配置：

| 配置项 | 值 |
|--------|------|
| Dart file | `inner/flutter_frontend_server/starter.dart` |
| Program arguments | 去掉 `dartaotruntime` 和 snapshot 路径后的参数（见下方） |
| Working directory | Example 工程根目录 |

Program arguments 示例（去掉前两段，保留从 `--sdk-root` 开始的部分，并加上 `--aop 1`）：

```
--sdk-root /path/to/flutter/bin/cache/artifacts/engine/common/flutter_patched_sdk_product/ --target=flutter --no-print-incremental-dependencies -Dflutter.inspector.structuredErrors=true -Ddart.vm.profile=false -Ddart.vm.product=true --aot --tfa --aop 1 --packages /path/to/example/.dart_tool/package_config.json --output-dill /path/to/example/.dart_tool/flutter_build/xxx/app.dill --depfile /path/to/example/.dart_tool/flutter_build/xxx/kernel_snapshot.d package:example/main.dart
```

> 关键：必须加 `--aop 1` 参数，否则 AOP transformer 不会执行。

#### 3. 设置断点

常用断点位置：

| 文件 | 位置 | 用途 |
|------|------|------|
| `aop_transformer_wrapper.dart:21` | `transform()` 入口 | 确认 transformer 被调用 |
| `aop_callimpl_transformer.dart` | `visitStaticInvocation` | 调试 Call 模式 |
| `aop_executeimpl_transformer.dart` | `transformMethodProcedure` | 调试 Execute 模式 |
| `aop_injectimpl_transformer.dart` | `transformMethodProcedure` | 调试 Inject 模式 |
| `aop_addimpl_transformer.dart` | `insertMethod4Class` | 调试 Add 模式 |

### 三、验证 hook 是否生效

```bash
# 导出编译后的 kernel AST 为文本
dart /path/to/flutter/bin/cache/dart-sdk/bin/snapshots/kernel_summary_tool.dart \
  /path/to/example/.dart_tool/flutter_build/xxx/app.dill \
  > output.dill.txt
```

打开 `output.dill.txt`，搜索被 hook 的方法名，确认 AST 已被修改。

---

## 版本对应关系

| Flutter | Dart SDK | Kernel Binary Version |
|---------|----------|----------------------|
| 3.10.5（旧） | 3.0.5 | 101 |
| 3.35.7（新） | 3.9.2 | 125 |
